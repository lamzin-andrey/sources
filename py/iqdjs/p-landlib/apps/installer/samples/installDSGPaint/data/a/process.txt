cd Z:/home/andrey/hdata/vdis/bridge/isya/xp-wall/dsgPaint/
Будем считать, что остался инсталлятор.

Пишем промпт.



-------------------------------------
Ну и png + bmp - в линукс.
Кажется закончил, проверить при "сохранении как".
И вообще проверить.
Точно в Temp треш оставляет.	*
Фильтр "Сохранить как" точно не готов	*
-------------------------------------
Запоминать открытый файл, чтобы не переспрашивать каждый раз - поддержка временного файла.
Пусть будет функция IsPngSupport()
Она будет возвращать булево, которое вычисляется при каждом запуске программы.

ТОгда, если расширение файла png, jpg, jpeg, то
 1 Вычисляем имя временного файла.						*
 2 Делаем exec path_to_real_file, path_to_temp_file                           *
 3 В tmpFile запоминаем то, что надо.						*
 4 В file запоминаем то, что надо.						*
 5 Работаем с tempFile, при каждом сохранении делаем exec path_to_temp_file, path_to_real_file (...)

Собственно, далее осталось реализовать конвертацию пайтоном и добить в лине.

Но. Хочется ещё динамически формировать фильтры в винде, если 
IsPngSupport === false


Ну и png + bmp - в линукс.
------------------------------------------
В следующий Запоминать параметры текста.
Запоминать открытый файл, чтобы не переспрашивать каждый раз.
-------------------------------------------
В следующий раз надо Инпут ввода толщины линии.
Запоминать параметры текста.
Запоминать открытый файл, чтобы не переспрашивать каждый раз.
-------------------------------------------
В следующий раз надо проработать приоритет внешнего буфера над внутрненим.
Там кажется всё непросто.
Возможно, избавиться от внутреннего буфера вовсе.
Что ещё. 
Инпут ввода толщины линии.
Запоминать параметры текста.
Запоминать открытый файл, чтобы не переспрашивать каждый раз.
Ну и png + bmp - в линукс.
------------------------------------------
В следующий раз я смещу палитру вправо.  
И надо проработать приоритет внешнего буфера над внутрненим.
Там кажется всё непросто.
Возможно, избавиться от внутреннего буфера вовсе.
Что ещё. 
Инпут ввода толщины линии.
Запоминать параметры текста.
Запоминать открытый файл, чтобы не переспрашивать каждый раз.
Ну и png + bmp - в линукс.
----------------------------------------
В следующий раз я сделаю перетаскивание вставленной области мышью.
Если insertSelectionMode
 то при клике внутри запоминаем позицию и факт клика.
 При up сбрасываем факт клика.
 При move перемещаем выделение, если факт клика
----------------------------------------
Зум. Точнее, рисование при зуме.

надо ещё добиться, чтобы вставляемый фрагмент умножался на зум не только при уменьшении.
Начал это делать в 
drawPastedFromInternalBuffer и всех причастных (в том числе и cutImage) но пока что-то не то

---------------------------------------
В следующий раз я сделаю управление выделением с контрола.
1. При выборе инструмента выделения должны показываться контролы.
2. При изменении размера выделения мышью в контролах должны менять значения.
3. При изменении значений в контролах должно меняться положение бокса. Планирую проверять isFocused

---------------------------

Сегодня я начинаю рисовать выделение на вставленном фрагменте.
Этапы:
1 Изменяем Paste так, чтобы он рисовал временный фрагмент, а не сразу постоянный.
   DrawTextOnImage рисует сразу в memoryDC
   DrawTextOverlay рисует не навечно на том DC, который ему передан.

2 Рисуем выделение поверх вставленного фрагмента
3 Определяем метод showMoveSelectionTool
4 Определяем переменную, которая подсказывает, что надо перерисовывать вставленный фрагмент при изменении инпутов.
5 При клике вне выделения прорисовываем на постоянку


---------------------------
Now i can add resize canvas button.
It make available text controls, but show only `w` and `h`.
And resize canvas when user change input values;
(Done 17 09 2025)

