… 1)
https://github.com/lamzin-andrey/sources/blob/master/php/test.loc/www/tz/netologia.loc/1.sql

этот запрос не вернет "записи в которой повторяются более одного раза 
(по полю login), отсортировать в любом порядке"


2) https://github.com/lamzin-andrey/sources/blob/master/php/test.loc/www/tz/netologia.loc/2/CViewHelper.php

при выводе значений, не кратных 4, не будет закрыт <tr>


3) хотелось увидеть логику подгрузки класса внутри еще одного класса, а не внутри application 
    (есть такой паттерн Strategy - он сюда хорошо подходит)

4) (не отправляя на сервер только так, наверное, т. е. я так понял что и яакс запрос отправлять нельзя??)

В ТЗ написано 
лишние проверки отправлять на сервер незачем 
